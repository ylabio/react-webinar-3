import React from 'react';

/**
 * Контекст для Store
 * @type {React.Context<Store>}
 */
export const StoreContext = React.createContext();

/**
 * Контекст для activeLanguage
 */

export const LanguageContext = React.createContext()
